import template from './login.html'
import './login.scss'

const name = 'login'

function controller() {

}

export default {
    name,
    options: {
        template,
        controller,
        controllerAs: 'self'
    }
}